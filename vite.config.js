import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/',
})
```

Note the capital **B** and capital **P** — it must match your repo name exactly.

---

**Step 2 — Commit and wait**
Scroll down → **Commit changes** → **Commit changes** again → go to **Actions** tab and wait for the green ✅

---

**Step 3 — Visit the correct URL**
Your site will be at:
```
https://oussrock.github.io/BasicPro/
